group 'uk.co.deliverymind'
version '1.0.0-SNAPSHOT'

apply plugin: 'maven'
apply plugin: 'java-gradle-plugin'

test {
    useTestNG()
    println """
If you execute this task for first time, it may take a good while to run.
This is because our tests cover compatibility with different Gradle versions, which may need to be downloaded first.
However, this will be handled automatically for you and will happen only once.
"""
}

dependencies {
    compile 'uk.co.deliverymind:lightning-core:5.5.1-SNAPSHOT'
    testCompile 'org.testng:testng:6.10'
}

repositories {
    mavenCentral()
    mavenLocal()
}